import styled from 'styled-components';

const Link = styled.span`
	align-self: center;
	font-size: 14px;
	color: #494949;
	margin-left: 50px;
	text-transform: uppercase;
	cursor: pointer;
`;

export default Link;
